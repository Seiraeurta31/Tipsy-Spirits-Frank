import { Schema } from 'mongoose'

const drinkSchema = new Schema({
  coctailId: Number,
  name: String,
  image: String,
  instructions: String,
  ingredients: [String],
})

export default drinkSchema